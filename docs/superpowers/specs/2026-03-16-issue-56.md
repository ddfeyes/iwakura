# Spec: SEARCH screen — global search across memory + diary history
**Issue:** #56
**Date:** 2026-03-16
**Author:** Lain (autonomous)

## Goal
Add a SEARCH // WIRED screen as the 6th orbital hub navigation node. Enables full-text search across memory files and diary chat history with PSX aesthetic styling.

## Backend

### GET /api/search?q=<term>
- Returns: `{ results: [...], total: N, query: str }`
- Each result: `{ source: "MEMORY"|"DIARY", file: str, snippet: str, timestamp: str, match_start: int, match_end: int }`
- Search: case-insensitive substring match
- Memory: iterate all .md files in ~/agents/lain/memory/ + MEMORY.md
- Diary: iterate load_diary_history() entries, search in text field
- Max 50 results, DIARY results first if query matches last message
- Return empty results (not 404) when no matches

## Frontend

### frontend/js/search.js
- Class: `SearchScreen`
- Input: `<input id="search-input" class="search-input" placeholder="QUERY //" autocomplete="off">`
- Debounce: 300ms after keyup
- Render results as PSX-styled list
- Source badge: `[MEMORY]` in purple `#8b7cc8`, `[DIARY]` in cyan `#00d4aa`
- Snippet: show 80-char context around match, bold the match term
- Empty: show `NO SIGNAL — WIRED SILENT`
- Loading: show `SCANNING WIRED...`
- Keyboard: ↑/↓ to highlight, Enter to navigate (MEMORY screen for memory hits, DIARY for diary hits)
- File code: `Sch011`

### frontend/index.html changes
- Add `<div id="screen-search" class="screen">` with search input + results container
- Add hub nav label `<div class="nav-label-item" data-target="search">` with code `Sch011` and name `SEARCH`

### frontend/js/nav.js change
- Add 6th node: `{ id: 'search', label: 'SEARCH', color: 0xff4488, baseAngle: Math.PI * 2 }`

### frontend/js/app.js changes
- Add `search: document.getElementById('screen-search')` to screens dict
- Add `SearchScreen` init when switching to search screen
- Global hotkey `/` → navigate to search + focus input

### frontend/css/style.css additions
- `.search-input`: full-width, monospace, transparent bg, orange border-bottom, no border-box
- `.search-results`: list of result rows
- `.result-row`: source badge + file + snippet + timestamp
- `.result-source-memory`: purple
- `.result-source-diary`: cyan
- `.search-match`: bold/highlighted match text
- `.search-empty`: centered, dim, monospace

## Acceptance
1. `GET /api/search?q=memory` returns memory file matches
2. `GET /api/search?q=hello` returns diary history matches  
3. Hub orbital ring shows 6 nodes including SEARCH (pink/magenta)
4. Hotkey `/` from any screen opens SEARCH
5. Results keyboard nav works (↑/↓/Enter)
6. Source badges visible and styled

## Approved
Self-approved (autonomous mode) — 2026-03-16
